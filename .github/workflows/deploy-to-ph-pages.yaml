name: CI

on:
  push:
    branches:
     - master
     - develop

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: install jekyll
      run: sudo gem install bundler jekyll
    - name: build
      run: jekyll build